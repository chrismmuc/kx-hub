# LLM Abstraction Layer Dependencies
# Supports multiple providers: Gemini, Claude (via Vertex AI)

# Gemini via Vertex AI
google-cloud-aiplatform>=1.38.0

# Claude via Vertex AI (optional - install with: pip install anthropic[vertex])
anthropic[vertex]>=0.40.0
